stages:
 - build

build-ubuntu:
  parallel:
    matrix:
      - UBUNTU_VERSION: [ "focal", "jammy", "noble", "resolute" ]
  stage: build
  image: gitlab.mpi-magdeburg.mpg.de/ci-images/minimal/ubuntu:${UBUNTU_VERSION}
  script:
    - apt update && apt install --yes libopenblas-dev libblas-dev liblapack-dev
    - mkdir -p build
    - cd build
    - cmake ../ -DDEBUG=ON
    - make
    - ctest -V


build-almalinux:
  tags:
    - haswell
  parallel:
    matrix:
      - ALMA_VERSION: ["8", "9", "10" ]
  stage: build
  image: gitlab.mpi-magdeburg.mpg.de/ci-images/minimal/almalinux:${ALMA_VERSION}
  script:
    - dnf install -y blas-devel lapack-devel
    - if [ ${ALMA_VERSION} -eq 9 ]; then (cd /usr/lib64/; ln -sf libblas.so.3.9.0 libblas.so.3; ln -sf liblapack.so.3.9.0 liblapack.so.3 ); fi
    - mkdir -p build
    - cd build
    - cmake ../ -DDEBUG=ON
    - make
    - ctest -V

build-fedora:
  parallel:
    matrix:
      - FEDORA_VERSION: ["40", "41", "42" ]
  stage: build
  image: gitlab.mpi-magdeburg.mpg.de/ci-images/minimal/fedora:${FEDORA_VERSION}
  script:
    - dnf install -y flexiblas-devel
    - mkdir -p build
    - cd build
    - cmake ../ -DDEBUG=ON
    - make
    - ctest -V



cross-win32:
  stage: build
  image: gitlab.mpi-magdeburg.mpg.de/ci-images/minimal/mingw:focal
  script:
    - bash ./tools/ci/cross-win32.sh

cross-win64:
  stage: build
  image: gitlab.mpi-magdeburg.mpg.de/ci-images/minimal/mingw:focal
  script:
    - bash ./tools/ci/cross-win64.sh

cross-win32-jammy:
  stage: build
  image: gitlab.mpi-magdeburg.mpg.de/ci-images/minimal/mingw:jammy
  script:
    - bash ./tools/ci/cross-win32.sh

cross-win64-jammy:
  stage: build
  image: gitlab.mpi-magdeburg.mpg.de/ci-images/minimal/mingw:jammy
  script:
    - bash ./tools/ci/cross-win64.sh


